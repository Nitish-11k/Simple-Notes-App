spring.datasource.url=jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:6543/postgres?serverTimezone=Asia/Kolkata&prepareThreshold=0
spring.datasource.driverClassName=org.postgresql.Driver
spring.datasource.username=postgres.krhdtzrcdrkflerqindm
spring.datasource.password=${DATABASE_PASSWORD:Nope2@13}

# Connection pool settings to fix prepared statement issues
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Disable H2 console since we're using PostgreSQL
spring.h2.console.enabled=false
server.port=8080

# Set timezone to IST
spring.jackson.time-zone=Asia/Kolkata
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Kolkata

spring.web.cors.allowed-origins=${FRONTEND_URL:http://localhost:3000}
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*

frontend.url=${FRONTEND_URL:http://localhost:3000}

logging.level.com.example.notesapp=INFO
logging.level.org.springframework.web=INFO
